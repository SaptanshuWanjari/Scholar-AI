// PaperCommandPalette is the inputs-namespace alias of CommandBar.
// Prefer importing from here when using it as a form/interaction component
// rather than as a navigation element.
export {
  CommandBar as PaperCommandPalette,
  type CommandBarProps as PaperCommandPaletteProps,
  type CommandEntry,
} from "../navigation/CommandBar";
